# Azure Database for MySQL Configuration Example
# 
# SETUP INSTRUCTIONS:
# 1. Copy this file to .env: cp .env.azure.example .env
# 2. Update all placeholder values with your actual Azure credentials
# 3. Ensure SSL certificate is downloaded: curl -o ssl/DigiCertGlobalRootCA.crt.pem https://cacerts.digicert.com/DigiCertGlobalRootCA.crt.pem
# 4. Test connection: python test_mysql_connection.py
# 5. Initialize database: python init_mysql_database.py

# Azure Database for MySQL Configuration
MYSQL_HOST=your-mysql-server.mysql.database.azure.com
MYSQL_PORT=3306
MYSQL_DATABASE=toefl_speaking_db
MYSQL_USERNAME=your-admin-username
MYSQL_PASSWORD=your-secure-password
MYSQL_SSL_MODE=REQUIRED
MYSQL_SSL_CA=ssl/DigiCertGlobalRootCA.crt.pem

# Constructed DATABASE_URL for MySQL (will be auto-generated by the application)
# DATABASE_URL=mysql+pymysql://${MYSQL_USERNAME}:${MYSQL_PASSWORD}@${MYSQL_HOST}:${MYSQL_PORT}/${MYSQL_DATABASE}

# Azure OpenAI Configuration
AZURE_OPENAI_API_KEY=your-azure-openai-api-key
AZURE_OPENAI_ENDPOINT=https://your-resource.cognitiveservices.azure.com/
AZURE_OPENAI_API_VERSION=2024-12-01-preview

# Azure OpenAI Model Deployments (replace with your actual deployment names)
# Examples: gpt-4, gpt-5-chat, whisper, gpt-4o-mini-tts
AZURE_OPENAI_GPT4_DEPLOYMENT=your-gpt4-deployment-name
AZURE_OPENAI_WHISPER_DEPLOYMENT=your-whisper-deployment-name
AZURE_OPENAI_TTS_DEPLOYMENT=your-tts-deployment-name

# Optional: CORS allowed origins (comma-separated)
ALLOWED_ORIGINS=http://localhost:3000,https://localhost:3000

# Optional: Rate limit per minute per user
RATE_LIMIT_PER_MINUTE=100

# Optional: Server port
PORT=8000

# Optional: Audio file storage path
# AUDIO_STORAGE_PATH=backend/audio_files